# 08: 会话恢复懒加载

**What to build:** 启动恢复上次会话时，仅当前激活标签立即创建 iframe，其余恢复的标签标记为待唤醒态（复用现有休眠唤醒 UI），首次点击激活时才加载。效果：上次开着多个标签时冷启动明显变快。LRU 保活上限逻辑不变。

**Blocked by:** None (can start immediately).

**Status:** resolved

- [x] 上次会话有 N（>1）个标签时，启动只发起 1 个页面加载
- [x] 其余标签首次点击时正常加载，休眠/唤醒 UI 表现与既有行为一致
- [x] 标签布局（顺序、固定、激活项）恢复正确

## Result

启动挂载时把恢复会话中的非激活标签置为休眠（清 src 快照），仅激活标签立即建 iframe；唤醒路径统一走 setActive（补 loading 标记）。LRU 休眠同步清快照，避免唤醒加载旧页。viewer 构建通过；冷启动效果待真机点验。
