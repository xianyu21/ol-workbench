# 10: 主进程日志轮转

**What to build:** 主进程日志写入前检查体积，超过阈值（约 1MB）时将当前日志改名为历史文件后重建，最多保留一份历史。长期托盘后台运行不再产生无限增长的日志文件。

**Blocked by:** None (can start immediately).

**Status:** resolved

- [x] 日志超过阈值后自动轮转，重建的新文件继续正常写入
- [x] 保留一份 `.old` 历史日志，更早的被丢弃
- [x] 轮转过程中的写入失败不影响应用运行

## Result

log() 写入前检查体积，超 1MB 轮转为 .old（最多一份历史）。主进程语法检查通过；轮转行为随真机运行验证。
