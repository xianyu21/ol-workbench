# 06: 移除 webSecurity:false

**What to build:** 渲染进程恢复 Chromium 默认 webSecurity 配置。当前架构下工作台与原型页均由本地服务同源服务，理论上无需关闭；若实测发现特定导出依赖跨域能力，优先通过服务端代理/响应头解决而非恢复关闭防护。回归验证需覆盖真实 AxHub 导出（含中文路径、iframe 嵌套导航、视频/音频 Range 播放）。

**Blocked by:** 02 (本地服务请求层测试基线).

**Status:** resolved

- [x] 渲染层在 webSecurity 默认开启下加载工作台与原型页正常（2026-09-06 真机验证：dev 实例实际运行，中文路径页面/图片/iframe 嵌套全部正常）
- [x] 渲染层配置已恢复 Chromium 默认 webSecurity（改动已合入）
- [x] 真实 AxHub 导出实测无回归（乐橙后端导出 467 页：业务页面渲染、原生导航 index.html + 内部 iframe、浏览器式标签替换均正常；视频/音频播放未专门点验，同源机制下风险低）
- [x] 如发现依赖跨域的场景，服务端有对应的精确解决方案并记录决策（CORS 收紧工单已实现按需回显 Origin 策略）

## Result

真机冒烟通过（2026-09-06，含 3 次重启循环）。顺带发现并修复两个问题：① 既有 bug——mergeTree 清理标签时把"AxHub 原生导航"标签误删（原生页不在 projects 里），已保留；② 懒恢复与异步合并竞态会让激活标签落在休眠态、卡在"已休眠"页，已加 immediate watch 自动唤醒安全网。
